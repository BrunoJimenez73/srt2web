# SRT2Web - Modular SRT Stream Processor
# =============================================
# Compatibilidad: Windows, macOS (Silicon/Intel), Linux
# =============================================
#
# Instalación:
#   Windows: Install.bat
#   macOS:   install_Mac.sh
#   Linux:   pip install -r requirements.txt
#
# NOTA: nvidia-ml-py se excluye automáticamente en Mac
#       (install_Mac.sh lo filtra - no funciona en Apple Silicon).
#       En Windows se instala vía Install.bat.

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pyyaml>=6.0
aiofiles>=23.0
websockets>=12.0
numpy>=1.24.0
pydub>=0.25.1
psutil>=5.9.0
platformdirs>=4.0.0
nvidia-ml-py>=12.0.0
python-dotenv>=1.0.0  # F112: load .env for SRT2WEB_JWT_SECRET
PyJWT>=2.8.0  # auth: JWT access/refresh tokens (core/auth_db.py)

# Pydantic v2 (required for this codebase)
pydantic>=2.0.0

# Processing (transcriber, translator, TTS)
faster-whisper>=1.0.0
argostranslate>=1.9.0
piper-tts>=1.2.0

# GPU Support
# Windows/Linux: onnxruntime-gpu con cuDNN 8.x (NO cuDNN 9.x)
# macOS:         onnxruntime-silicon (CoreML) - instalado por install_Mac.sh
# IMPORTANTE: nvidia-cudnn-cu11 (NO cu12 - tiene cuDNN 9.x incompatible)
# onnxruntime-gpu>=1.19.0

# WebRTC Support
aiortc>=1.14.0

# TTS Engines
edge-tts>=6.1.0

# CLI/TUI dependencies
requests>=2.31.0
httpx>=0.27.0
click>=8.0.0
rich>=13.0.0
textual>=0.52.0
colorama>=0.4.6

# Tooling / harness
ruff>=0.3.0
mkdocs>=1.6.0
